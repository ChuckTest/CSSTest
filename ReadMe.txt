1.Create a website named as Test in IIS 
2.Add a application for the website
3.Alias named as CSS, create a new application pool or use the existing one
4.Set the physical path to project folder under this repository